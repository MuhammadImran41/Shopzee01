# Nixpacks configuration for Railway deployment
# This tells Railway exactly how to build and run .NET 9 app

[phases.setup]
nixPkgs = ["dotnet-sdk_9"]

[phases.build]
cmds = ["dotnet publish -c Release -o /app/publish"]

[start]
cmd = "dotnet /app/publish/Shopzee.API.dll"
