# Nixpacks configuration — build from backend/Shopzee.API

[phases.setup]
nixPkgs = ["dotnet-sdk_9"]

[phases.build]
cmds = [
  "cd backend/Shopzee.API && dotnet publish -c Release -o /app/publish"
]

[start]
cmd = "dotnet /app/publish/Shopzee.API.dll"
